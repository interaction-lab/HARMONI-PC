<!-- This is an example launch file for how a ROS application could use a node -->
<launch>
   
	<arg name="use_camera" default="true"/>
	<arg name="test" default="false"/>
	<arg name="input_test" default=""/>
	<arg name="id_test" default="def"/>
  	<group if="$(arg use_camera)">
			<rosparam file="$(find pc_camera)/config/configuration.yaml"/>
			<param name="test" value="$(arg test)"/>
			<param name="input_test" value="$(arg input_test)"/>
			<param name="id_test" value="$(arg id_test)"/>
			<node pkg="pc_camera" type="camera_service.py" name="pc_camera" output="screen"/>
  	</group>


</launch>
